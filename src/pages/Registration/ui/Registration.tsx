import React from 'react';
import { Register } from '@widgets/Register';

import { SBaseContainer } from './registration.styles';

const Registration = () => {
  return (
    <SBaseContainer>
      <Register />
    </SBaseContainer>
  );
};

export default Registration;
